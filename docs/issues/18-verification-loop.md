# Issue #18: 验证闭环（采纳效果追踪）

> Parent: 期6 改进清单（grill Q10：只展示不判决；按根因类别追踪；按周归一化）

## What to build

- **追踪口径**：按根因类别，不按具体推荐物——装了 TDD skill 后看「测试验证缺失」类信号频率变化，不看 skill 调用遥测
- **对比**：采纳前 90 天该类信号次数（installations 表基线）vs 采纳后至今，**按周归一化**（采纳后只有 2 周数据也能比）
- **状态标记**：30 天后自动标 生效中 / 未生效 / 已卸载；不足 30 天或基线为 0 时第四态「观察中」（不判决，实现增补）；不做统计显著性，不自动卸载
- **归因声明**：UI 固定文案「信号下降 ≠ 推荐生效（可能是项目收尾）」——当信号不当判决，守住口径信任链
- 已采纳历史卡片显示前后对比数字 + 状态；未生效项降权（不再推荐同类）

## Acceptance criteria

- [x] 对比数字按周归一化，数据不足时明确提示
- [x] 归因声明文案在每张已采纳卡片可见
- [x] 三态自动流转（含 uninstalled 由 #17 撤销触发）
- [x] 测试覆盖归一化计算与三态流转

## Blocked by

- #17 安装治理（依赖 installations 基线快照）
